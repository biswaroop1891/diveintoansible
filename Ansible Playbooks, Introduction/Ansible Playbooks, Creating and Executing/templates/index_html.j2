<!doctype html>
<html>
	<head>
		<title>My Custom Website</title>
		<meta charset="utf-8"/>
		<meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1"/>
      <style type="text/css">* {font-family: Inter;} body {padding: 2em;}</style>
	</head>
	<body>
		<div>
			<h1>Hello world! from {{ ansible_all_ipv4_addresses[1] }}:{{ ansible_hostname }}</h1>

            {% if ENABLE_SSL is defined %}
            {#this block gets added when ssl is enabled (ENABLE_SSL ??)#}
            <p>I have SSL enabled</p>
            {% endif %}
		</div>
	</body>
</html>